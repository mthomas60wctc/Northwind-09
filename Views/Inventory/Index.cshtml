<div id="User" style="display:none;" data-email="@User.Identity.Name"
    data-customer="@User.IsInRole("northwind-customer")"></div>
<table class="table table-hover">
    <thead>
        <tr>
            <th scope="col" class="text-end"> ID

            </th>
            <th scope="col" class="text-start"> Product

            </th>
            <th scope="col">
                <div class="input-group">
                    <input class="form-check-input ms-2 mt-2" type="checkbox" id="instock" checked>
                    <label class="form-check-label ms-2 mt-1" for="instock">In Stock</label>
                    <input class="form-check-input ms-2 mt-2" type="checkbox" id="restock" checked>
                    <label class="form-check-label ms-2 mt-1" for="restock">Restock</label>
                    <input class="form-check-input ms-2 mt-2" type="checkbox" id="outofstock" checked>
                    <label class="form-check-label ms-2 mt-1" for="outofstock">Out of Stock</label>
                </div>
            </th>
            <th scope="col" class="text-end">Price ($)</th>
            <th scope="col" class="text-end">Reorder Level</th>
            <th scope="col" class="text-end">Units on Order</th>
            <th scope="col" class="text-end">Stock</th>
        </tr>
    </thead>
    <tbody id="product_rows" data-id="@ViewBag.id">
    </tbody>
</table>
<div id="cartModal" class="modal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Edit Item</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="row">
                    <div class="col col-xs-5"><strong>ID</strong></div>
                    <div class="col col-xs-5 text-left"><strong>Product</strong></div>
                </div>
                <div class="row">
                    <div class="col col-xs-5 pt-2" id="ProductId">-1</div>
                    <div class="col col-xs-5 pt-2">
                        <input type="text" id="ProductName" class="form-control">
                    </div>
                </div>
                <br>
                <div class="row">
                    <div class="col col-xs-2 text-right"><strong>Price</strong></div>
                    <div class="col col-xs-2 text-right"><strong>Reorder Level</strong></div>
                    <div class="col col-xs-2 text-right"><strong>Units on Order</strong></div>
                    <div class="col col-xs-2 text-right"><strong>Units in Stock</strong></div>
                </div>
                <div class="row">
                    <div class="col col-xs-2">
                        <input type="number" min="1" value="1" id="UnitPrice" class="form-control" />
                    </div>
                    <div class="col col-xs-2">
                        <input type="number" min="1" value="1" id="ReorderLevel" class="form-control" />
                    </div>
                    <div class="col col-xs-2">
                        <input type="number" min="1" value="1" id="OnOrder" class="form-control" />
                    </div>
                    <div class="col col-xs-2">
                        <input type="number" min="1" value="1" id="InStock" class="form-control" />
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-primary" id="addToCart">Continue</button>
            </div>
        </div>
    </div>
</div>
<div class="toast-container position-fixed bottom-0 end-0 p-3">
    <div id="liveToast" class="toast" role="alert" aria-live="assertive" aria-atomic="true">
        <div class="toast-header">
            <strong class="me-auto" id="toast_header">Toast Header</strong>
            <button type="button" class="btn-close" data-bs-dismiss="toast" aria-label="Close"></button>
        </div>
        <div class="toast-body" id="toast_body">
            Toast Message
        </div>
    </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
<script src="../../inventory.js"></script>